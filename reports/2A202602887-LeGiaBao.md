# Individual contribution report

## Thông tin

- Họ và tên: Lê Gia Bảo
- Mã học viên: 2A202602887
- Nhóm: K4-L3A
- Repository/branch:  `https://github.com/oabga/K4-L3A-RAG-Pipeline/tree/contrib/LeGiaBao-02887`

## Phần việc đã thực hiện


| Module/deliverable | Việc tôi trực tiếp làm                                                                                                                   | File/commit/PR                                                                             | Trạng thái |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ | ---------- |
| Task 2 crawl news  | Viết crawler Crawl4AI (selector theo domain, fallback Báo Đồng Nai khi Báo Mới chặn), thu 3 bài thuế hộ kinh doanh                       | `src/task2_crawl_news.py`; `data/landing/news/article_06.json`–`08.json`; commit `18371d2` | Done       |
| Corpus nhóm        | Merge legal/news từ baovq + hoang-minh, giữ 8 JSON không đè file, chuẩn hóa news markdown                                                | `8fae3a0`, `e8a28b7`; `data/landing/`, `data/standardized/news/`                           | Done       |
| Task 10 + UI       | Generation có citation, system prompt chỉ dùng context đã retrieve, chitchat/out-of-domain refusal; UI Streamlit LexRAG hiện nguồn/score | `src/task10_generation.py`, `app.py`                                                       | Done       |
| Evaluation         | Golden 18 câu grounded; A/B dense vs hybrid+RRF; điền RESULT                                                                             | `group_project/evaluation/golden_dataset.json`, `run_ab.py`, `RESULT.md`                   | Done       |


Task 1/4/5/6/7/8/9 do thành viên khác; tôi merge corpus vào branch rồi làm Task 2, Task 10, UI và evaluation.

## Quyết định kỹ thuật quan trọng

1. **Quyết định:** Crawler Task 2 dùng Crawl4AI + CSS selector + fallback URL gốc khi Báo Mới fail.
  **Lý do/evidence:** `CANONICAL_FALLBACKS` trong `task2_crawl_news.py`; 3 JSON đủ `url/title/date_crawled/content_markdown`, acceptance test news ≥5 sau khi gộp corpus.  
   **Trade-off:** Không crawl được Thư viện Pháp luật (Cloudflare); 5 bài đó lấy sẵn từ hoang-minh, không giả lập crawl thành công.
2. **Quyết định:** LLM chỉ nhận context đã retrieve; không có overlap/chào hỏi thì refusal, không dump chunk đầu.
  **Lý do/evidence:** `SYSTEM_PROMPT` + `_is_chitchat` + `_has_sufficient_evidence` trong `task10_generation.py`. Query “Xin chào” trước đó trả TT-34/NĐ 349 sai.  
   **Trade-off:** Extractive khi thiếu `.env` key dễ lệch câu hiệu lực giữa nhiều nghị định; cần OpenAI embedding 1536-d trùng collection.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng: `pytest -q` (20 passed); in-domain “Nghị định 349/2026/NĐ-CP có hiệu lực thi hành khi nào?”; out-of-domain “Cách nấu phở bò Hà Nội ngon?”; greeting “Xin chào”.
- Kết quả trước/sau nếu có: greeting trước dump chunk pháp luật / sau hướng dẫn hỏi corpus; A/B hybrid+RRF 0.900 > dense-only 0.880 trên 18 golden (`RESULT.md`).
- Lỗi đã phát hiện và cách xử lý: Chroma `expecting 384, got 1536` vì index hashing khi chưa có key rồi query OpenAI — xóa collection, `python -m src.task4_chunking_indexing` lại với `text-embedding-3-small`.

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: extractive/hashing lẫn điều “hiệu lực thi hành” của NĐ 68/254 khi hỏi NĐ 349 nếu chưa embed OpenAI.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: chunk theo Điều/khoản và giữ số hiệu văn bản trong metadata (worst case Luật 133 và TT-34 trong `RESULT.md`).

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 2026-09-21
- Tên thành viên: Lê Gia Bảo

